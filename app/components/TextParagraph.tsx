import React from 'react';

const Par = ({ children }) => {
  return (
    <p className="text-black text-base leading-6 px-4 py-5 rounded tracking-wide font-light ">
      {children}
    </p>
  );
};

export default Par;

// font-extralight max-w-[400px] text-xl
